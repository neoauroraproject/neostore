# Official first-party extensions

P0 loads these through the Extension Host (same rules as third-party).
Examples to add during build: `payment-manual-bank`, `payment-cryptomus`, `delivery-manual`, `delivery-entitlement-code`, base `product-type-*`.
