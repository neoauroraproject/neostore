# @neostore/sdk

Public Extension SDK package (Interfaces · Events · Hooks · API clients · CLI · test utils).

**Ships fully in Phase 2 / Platform v2.**  
P0 keeps Host stubs inside `apps/api`; this package is reserved for the developer-facing SDK.
